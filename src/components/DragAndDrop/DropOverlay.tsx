import styled from 'styled-components'

export const DropOverlay = styled.div`
  top: 0;
  position: absolute;
  width: 100%;
  min-height: 100%;
  background: var(--F_Primary_Overlay);
  pointer-events: none;
  z-index: 999;
`
